# OderManagement
The Order Management System is an iOS app that enables users to manage orders. It includes user authentication (registration/login) and order management features such as viewing, adding, editing, and deleting orders, with data persistence using Core Data
